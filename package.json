{
  "name": "v6",
  "version": "0.0.1",
  "description": "A rendering library: write the same code for both 2D and WebGL contexts.",
  "main": "src/v6.js",
  "scripts": {
    "test": "node -e \"console.log( require( './src/colors/hsla' )( 'magenta' ).shade( -25 ).rgba().shade( 25 ).toString() === 'rgba(255, 0, 255, 1)' ? 'It works.' : 'It not works.' );\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/silent-tempest/v6.git"
  },
  "author": "SILENT",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/silent-tempest/v6/issues"
  },
  "homepage": "https://github.com/silent-tempest/v6#readme",
  "dependencies": {
    "peako": "^0.0.1",
    "platform": "^1.3.5"
  },
  "browserify": {
    "transform": [
      "browserify-optional"
    ]
  }
}
